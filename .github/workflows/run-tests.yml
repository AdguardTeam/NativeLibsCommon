name: Run tests

on:
  push:
    branches:
      - master
      - 'stable-*'
      - 'dev-*'
  pull_request:
  workflow_dispatch:

jobs:
  win-build:
    name: Win build
    if: github.repository == 'AdGuardSoftwareLimited/native-libs-common'
    permissions:
      contents: read
      id-token: write
    secrets: inherit
    uses: AdGuardSoftwareLimited/actions/.github/workflows/junit-tests.yml@master
    with:
      runs-on: '["ephemeral","ephemeral-vmwin","${{ vars.BUILD_IMAGE_VERSION_WINDOWS }}","ephemeral-limit-memory-4096","ephemeral-limit-cpu-4"]'
      container-image: ''
      test-command: |
        set -e
        set -x

        conan remote add --index 0 art ${{ vars.CONAN_REPO_URL }} || true

        # Import 32-bit MSVC environment into bash.
        # Skip variables whose names are not valid bash identifiers
        # (e.g. ProgramFiles(x86)), since MSVC vars we actually need
        # (INCLUDE, LIB, LIBPATH, PATH, VCToolsInstallDir, ...) are valid.
        while IFS= read -r line; do
          [[ -z "$line" ]] && continue
          key="${line%%=*}"
          value="${line#*=}"
          [[ -z "$key" ]] && continue
          [[ "$key" =~ ^[a-zA-Z_][a-zA-Z0-9_]*$ ]] || continue
          export "$key=$value"
        done < <(cmd /c "vcvarsamd64_x86 & set" | tr -d '\r')

        mkdir -p build
        cd build
        cmake -DCMAKE_BUILD_TYPE=Debug \
          -DCMAKE_C_COMPILER="cl.exe" \
          -DCMAKE_CXX_COMPILER="cl.exe" \
          -DCMAKE_C_FLAGS_DEBUG=/MT \
          -DCMAKE_CXX_FLAGS_DEBUG=/MT \
          -G Ninja ..
        ninja tests
        ctest --output-junit junit.xml -D ExperimentalTest --no-compress-output

        conan remote login art "${{ secrets.ARTIFACTORY_USER }}" --password "${{ secrets.ARTIFACTORY_API_KEY }}"
        conan upload -r art -c "*" 2>&1 | grep --line-buffered "Uploading"
      results-path: 'build/junit.xml'
      artifact-name: 'test-results-windows'

  linux-build:
    name: Linux build
    if: github.repository == 'AdGuardSoftwareLimited/native-libs-common'
    permissions:
      contents: read
      id-token: write
    secrets: inherit
    uses: AdGuardSoftwareLimited/actions/.github/workflows/junit-tests.yml@master
    with:
      runs-on: '["ephemeral","ephemeral-linux","${{ vars.BUILD_IMAGE_VERSION_CORELIBS_BUILDER }}","ephemeral-limit-memory-8192","ephemeral-limit-cpu-6"]'
      container-image: 'adguard/core-libs:2.10'
      test-command: |
        set -e
        set -x

        python3 -m pip install conan --upgrade
        conan remote add --index 0 art ${{ vars.CONAN_REPO_URL }} || true

        mkdir -p build
        cd build
        cmake -DCMAKE_BUILD_TYPE=Debug -GNinja \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_CXX_FLAGS="-stdlib=libc++" \
          -DSANITIZE=yes \
          ..
        ninja tests
        ctest --output-junit junit.xml -D ExperimentalTest --no-compress-output

        conan remote login art "${{ secrets.ARTIFACTORY_USER }}" --password "${{ secrets.ARTIFACTORY_API_KEY }}"
        conan upload -r art -c "*" 2>&1 | grep --line-buffered "Uploading"
      results-path: 'build/junit.xml'
      artifact-name: 'test-results-linux'

  mac-build:
    name: Mac build
    if: github.repository == 'AdGuardSoftwareLimited/native-libs-common'
    permissions:
      contents: read
      id-token: write
    secrets: inherit
    uses: AdGuardSoftwareLimited/actions/.github/workflows/junit-tests.yml@master
    with:
      runs-on: '["ephemeral","ephemeral-mac","${{ vars.BUILD_IMAGE_VERSION_MACOS }}","ephemeral-limit-memory-8192","ephemeral-limit-cpu-4"]'
      container-image: ''
      test-command: |
        set -e
        set -x

        rm -rf venv
        python3 -m venv venv
        source ./venv/bin/activate

        python3 -m pip install conan
        conan remote add --index 0 art ${{ vars.CONAN_REPO_URL }} || true

        ARCH=$(uname -m)
        mkdir -p build
        cd build
        cmake -DCMAKE_BUILD_TYPE=Debug -GNinja \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_CXX_FLAGS="-stdlib=libc++" \
          -DCMAKE_OSX_ARCHITECTURES="$ARCH" \
          ..
        ninja tests
        ctest --output-junit junit.xml -D ExperimentalTest --no-compress-output

        source ./venv/bin/activate
        conan remote login art "${{ secrets.ARTIFACTORY_USER }}" --password "${{ secrets.ARTIFACTORY_API_KEY }}"
        conan upload -r art -c "*" 2>&1 | grep --line-buffered "Uploading"
      results-path: 'build/junit.xml'
      artifact-name: 'test-results-macos'

  android-build:
    name: Android build
    if: github.repository == 'AdGuardSoftwareLimited/native-libs-common'
    runs-on:
      - ephemeral
      - ephemeral-linux
      - ${{ vars.BUILD_IMAGE_VERSION_CORELIBS_BUILDER }}
      - ephemeral-limit-memory-8192
      - ephemeral-limit-cpu-6
    container:
      image: adguard/core-libs:2.10
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Build Android library
        run: |
          python3 -m pip install conan --upgrade
          conan remote add --index 0 art ${{ vars.CONAN_REPO_URL }} || true
          cd android/NativeLibsCommon
          touch local.properties
          CMAKE_DIR=$(dirname $(dirname $(which cmake)))
          echo cmake.dir=$CMAKE_DIR >> local.properties
          ./gradlew :lib:assembleRelease

      - name: Upload to Conan
        if: always()
        run: |
          conan remote login art "${{ secrets.ARTIFACTORY_USER }}" --password "${{ secrets.ARTIFACTORY_API_KEY }}"
          conan upload -r art -c "*" 2>&1 | grep --line-buffered "Uploading"
