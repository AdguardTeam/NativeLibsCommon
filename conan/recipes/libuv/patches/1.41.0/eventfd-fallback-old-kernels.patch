diff --git a/src/unix/async.c b/src/unix/async.c
index 0e1632f..a8b3c4d 100644
--- a/src/unix/async.c
+++ b/src/unix/async.c
@@ -207,10 +207,17 @@ static int uv__async_start(uv_loop_t* loop) {
     return 0;

 #ifdef __linux__
+  /* Try eventfd with flags first (requires kernel 2.6.27+) */
   err = eventfd(0, EFD_CLOEXEC | EFD_NONBLOCK);
-  if (err < 0)
-    return UV__ERR(errno);
-
-  pipefd[0] = err;
-  pipefd[1] = -1;
+  if (err >= 0) {
+    pipefd[0] = err;
+    pipefd[1] = -1;
+  } else if (errno == EINVAL || errno == ENOSYS) {
+    /* Fallback to pipe for old kernels without EFD_CLOEXEC/EFD_NONBLOCK */
+    err = uv__make_pipe(pipefd, UV_NONBLOCK_PIPE);
+    if (err < 0)
+      return err;
+  } else {
+    return UV__ERR(errno);
+  }
 #else
   err = uv__make_pipe(pipefd, UV_NONBLOCK_PIPE);
   if (err < 0)
