diff --git a/crypto/internal.h b/crypto/internal.h
index 7c4b509..2f61cf1 100644
--- a/crypto/internal.h
+++ b/crypto/internal.h
@@ -847,7 +847,7 @@ inline void *OPENSSL_memchr(void *s, int c, size_t n) {
     return nullptr;
   }
 
-  return memchr(s, c, n);
+  return (void *)memchr(s, c, n);
 }
 
 inline int OPENSSL_memcmp(const void *s1, const void *s2, size_t n) {
