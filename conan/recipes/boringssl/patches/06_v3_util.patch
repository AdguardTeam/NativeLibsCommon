diff --git a/crypto/x509/v3_utl.cc b/crypto/x509/v3_utl.cc
index 6605c5c..6966455 100644
--- a/crypto/x509/v3_utl.cc
+++ b/crypto/x509/v3_utl.cc
@@ -709,7 +709,7 @@ static int wildcard_match(const unsigned char *prefix, size_t prefix_len,
   // Check that the part matched by the wildcard contains only
   // permitted characters and only matches a single label.
   for (p = wildcard_start; p != wildcard_end; ++p) {
-    if (!OPENSSL_isalnum(*p) && *p != '-') {
+    if (!OPENSSL_isalnum(*p) && *p != '-' && *p != '_') {
       return 0;
     }
   }
@@ -936,7 +936,10 @@ static int do_x509_check(const X509 *x, const char *chk, size_t chklen,
         break;
       }
     }
-    return rv;
+    // Added by AdGuard. Google removed X509_CHECK_FLAG_ALWAYS_CHECK_SUBJECT.
+    if (rv != 0) {
+      return rv;
+    }
   } else if (critical != -1) {
     // Syntax error in the subjectAltName extension.
     return 0;
